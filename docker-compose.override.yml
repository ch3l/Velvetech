version: '3.4'

services:
  velvetech.api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    
    volumes:
      - ${APPDATA}/Microsoft/UserSecrets:/root/.microsoft/usersecrets:ro
  velvetech.web:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      #- SERVICE1=http://services1:80
    ports:
      - "80"

